<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yusheng.mapper.BedsMapper">

<!--    查询全部床位情况-->
    <select id="findAll" resultType="com.yusheng.pojo.Bed">
        select id, room_number, bed_number, status, created_at, updated_at
        from beds
        order by updated_at desc
    </select>

<!--    删除床位-->
    <delete id="deleteById">
        delete from beds where id = #{id}
    </delete>

<!--    新增床位-->
    <insert id="save">
        insert into beds(room_number, bed_number, status, created_at, updated_at)
        values (#{roomNumber}, #{bedNumber}, #{status}, #{createdAt}, #{updatedAt})
    </insert>

<!--    根据ID查询床位信息-->
    <select id="getById" resultType="com.yusheng.pojo.Bed">
        select id, room_number, bed_number, status, created_at, updated_at
        from beds
        where id = #{id}
    </select>

<!--    修改床位-->
    <update id="updateById">
        update beds
        <set>
            <if test="roomNumber != null and roomNumber != ''">room_number = #{roomNumber},</if>
            <if test="bedNumber != null and bedNumber != ''">bed_number = #{bedNumber},</if>
            <if test="status != null and status != ''">status = #{status},</if>
            <if test="updatedAt != null">updated_at = #{updatedAt}</if>
        </set>
        where id = #{id}
    </update>
</mapper>